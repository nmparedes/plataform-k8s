{
  "name": "fiap-tech-challenge-platform-k8s",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Kubernetes platform configuration for FIAP Tech Challenge Phase 4.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "render:kong": "node scripts/render-kong-config.mjs",
    "validate:kong": "node scripts/validate-kong-config.mjs",
    "validate:k8s": "node scripts/validate-k8s-manifests.mjs && node scripts/validate-rabbitmq-definitions.mjs",
    "validate": "npm run validate:kong && npm run validate:k8s"
  },
  "license": "UNLICENSED"
}
